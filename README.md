# record-replay

### Device streaming
Run `WS-SCRCPY` project and get the device streaming link from that OR run `SCRCPY` binary on host

Replace device streaming link inside the index.html


### Appium Server 

Run appium server at port 4723.


### Start Server 

Run `app.py` using python and open web console on `http://localhost:5000`
